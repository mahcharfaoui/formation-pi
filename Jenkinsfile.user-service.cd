pipeline {
    agent any

    parameters {
        string(name: 'DOCKER_TAG', defaultValue: 'latest', description: 'Tag Docker a deployer (ex: 42 ou latest)')
    }

    environment {
        DOCKER_IMAGE = "asmaamh/user-service:${params.DOCKER_TAG}"
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'master',
                    url: 'https://github.com/mahcharfaoui/formation-pi.git',
                    credentialsId: 'github-credentials'
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                sh "kubectl create deployment user-service -n plateforme --image=${DOCKER_IMAGE} --dry-run=client -o yaml | kubectl apply -f -"
            }
        }

        stage('Verify Deployment') {
            steps {
                sh "kubectl rollout status deployment/user-service -n plateforme"
            }
        }
    }

    post {
        failure {
            emailext(
                subject: "[CD] ECHEC - user-service deploy #${BUILD_NUMBER}",
                body: "Le deploiement user-service tag ${params.DOCKER_TAG} a echoue.",
                to: 'ton-email@gmail.com'
            )
        }
        success {
            emailext(
                subject: "[CD] SUCCES - user-service deploy #${BUILD_NUMBER}",
                body: "Deploiement reussi: ${DOCKER_IMAGE}",
                to: 'ton-email@gmail.com'
            )
        }
    }
}
