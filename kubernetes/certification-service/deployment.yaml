apiVersion: apps/v1
kind: Deployment
metadata:
  name: certification-service
  labels:
    app: certification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: certification-service
  template:
    metadata:
      labels:
        app: certification-service
    spec:
      containers:
        - name: certification-service
          image: plateforme/certification-service:latest
          ports:
            - containerPort: 8087
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/certification_db"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka-server:8761/eureka/"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: certification-service
spec:
  selector:
    app: certification-service
  ports:
    - port: 8087
      targetPort: 8087
  type: ClusterIP
