apiVersion: apps/v1
kind: Deployment
metadata:
  name: formateur-service
  labels:
    app: formateur-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: formateur-service
  template:
    metadata:
      labels:
        app: formateur-service
    spec:
      containers:
        - name: formateur-service
          image: plateforme/formateur-service:latest
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/formateur_db"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka-server:8761/eureka/"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: formateur-service
spec:
  selector:
    app: formateur-service
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP
